name: Stage5CI

on: [push, pull_request]

jobs:
#  test:
#    runs-on: ubuntu-latest
#
#    services:
#      postgres:
#        image: postgres:17
#        env:
#          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
#          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
#          POSTGRES_DB: DjangoStage5
#        options: >-
#          --health-cmd pg_isready
#          --health-interval 10s
#          --health-timeout 5s
#          --health-retries 5
#        ports:
#          - 5432:5432
#      redis:
#        image: redis:7-alpine
#        options: >-
#          --health-cmd "redis-cli ping"
#          --health-interval 10s
#          --health-timeout 5s
#          --health-retries 5
#        ports:
#          - 6379:6379
#
#    steps:
#    - name: Check out code
#      uses: actions/checkout@v3
#
#    - name: Set up Python
#      uses: actions/setup-python@v4
#      with:
#        python-version: '3.12'
#
#    - name: Install dependencies
#      run: |
#        python -m pip install --upgrade pip
#        pip install poetry
#        poetry install --no-interaction --no-root
#
#    - name: Run migrations
#      env:
#        NAME: DjangoStage5
#        USER: postgres
#        PASSWORD: 6570
#        HOST: localhost
#        DATABASE_URL: postgres://postgres:postgres@localhost:5432/test_db
#        SECRET_KEY: test-secret-key-for-ci-12345
#        DEBUG: "False"
#        DJANGO_SETTINGS_MODULE: config.settings
#      run: poetry run python manage.py migrate
#
#    - name: Run tests
#      env:
#        NAME: DjangoStage5
#        USER: ${{ secrets.POSTGRES_USER }}
#        PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
#        HOST: localhost
#        DATABASE_URL: postgres://postgres:postgres@localhost:5432/test_db
#        SECRET_KEY: test-secret-key-for-ci-12345
#        DEBUG: "False"
#        DJANGO_SETTINGS_MODULE: config.settings
#        CELERY_BROKER_URL: redis://localhost:6379/0
#        CELERY_RESULT_BACKEND: redis://localhost:6379/0
#        REDIS_URL: redis://localhost:6379/0
#      run: poetry run python manage.py test

  deploy:
    runs-on: ubuntu-latest

#    needs: test

    steps:
      - name: Check SSH key format
        run: |
          echo "=== Оригинальный ключ ==="
          echo "${{ secrets.SSH_KEY }}"
          echo "=== Длина ключа ==="
          echo -n "${{ secrets.SSH_KEY }}" | wc -c
          echo "=== Первые 10 строк ==="
          echo "${{ secrets.SSH_KEY }}" | head -10
          echo "=== Hex dump первых 100 байт ==="
          echo -n "${{ secrets.SSH_KEY }}" | head -c 100 | xxd
      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: Copy project files to server
        run: |
          rsync -avz --exclude "__pycache__" . ${{ secrets.SSH_USER }}@${{ secrets.SERVER_IP }}:${{ secrets.DEPLOY_DIR }}
      - name: Install dependensies on server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SERVER_IP }} << 'EOF'
            cd ${{ secrets.DEPLOY_DIR }}
            poetry install
          EOF
      - name: Apply migrations on server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SERVER_IP }} << 'EOF'
          cd ${{ secrets.DEPLOY_DIR }}
          poetry run python manage.py migrate
          EOF

      - name: Collect static files on server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SERVER_IP }} << 'EOF'
            cd ${{ secrets.DEPLOY_DIR }}
            poetry run python manage.py collectstatic --noinput
          EOF

      - name: Restart application
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SERVER_IP }} << 'EOF'
          sudo systemctl restart myapp.service
          EOF